namespace DotnetWorker.Infrastructure.ExampleService;

// Example, but your interface should be in Domain or Application layer (where appropriate)
public interface IExampleService
{
    void DoSomething();
}
